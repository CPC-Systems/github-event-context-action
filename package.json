{
  "name": "@cpc-systems/github-event-context-action",
  "version": "1.2.1",
  "description": "GitHub Action to collect conformed information on the workflow event context. Forked from Ably's creation.",
  "scripts": {
    "lint": "eslint .",
    "package": "ncc build server.js -o dist --source-map --license licenses.txt",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": "CPC-Systems/github-event-context-action",
  "license": "Apache-2.0",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "@vercel/ncc": "^0.38.1",
    "eslint": "^8.57.0",
    "eslint-config-airbnb": "^19.0.4",
    "eslint-plugin-import": "^2.29.1"
  }
}
